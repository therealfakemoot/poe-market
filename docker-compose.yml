version: '3'
services:
  prometheus:
    build:
      context: .
      dockerfile: prom-dockerfile
    ports:
      - "9091:9090"
  poe:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9092:9092"
